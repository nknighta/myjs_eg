#include "js_engine.h"
#include <iostream>

void SimpleJSEngine::execute(const std::string& script) {
    // ここにJavaScriptを実行する実際のコードを実装します。
    // 現在は、スクリプトをコンソールに出力するだけです。
    std::cout << "Executing script: " << script << std::endl;
}
